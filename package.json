{
  "name": "exercises",
  "private": true,
  "description": "some javascript exercises",
  "scripts": {
    "test": "echo \"Error: run npm test from inside the challenge directories or do 'npm run test-all'\" && exit 1",
    "test-all": "mocha '**/test.js' --exclude 'node_modules/**'"
  },
  "author": "Moshe Kolodny",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^5.2.0",
    "sinon": "^1.15.3"
  }
}
